/**
 * Точки расширения этапов 2–3 (SPEC.md, раздел 11). Пока выключены.
 * Включение — одна строка здесь плюс реализация компонента.
 */
export const features = {
  /** Блок расчёта окупаемости на карточке объекта (/calculator и виджет) */
  calculator: false,
  /** Казахская локаль: также добавить "kk" в site.site.locales и заполнить messages/kk.json */
  localeKk: false,
} as const;
